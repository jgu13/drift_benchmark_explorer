# C3.1 — BFCL CLI-Semantics Drift: Execution Plan

## 0. Goal and roster contract

Build an executable BFCL-derived **C3.1** family for CLI semantics.

**Roster contract**

- Family: `CLI semantics`
- Origin: BFCL v4 pattern
- Skill store: versioned command grammar, flag semantics, compatibility constraints, recipes
- Application recipe: select current command form, execute, verify output before reuse
- Drift: `flag_remap + deprecate + shortcut_add`
- Topology: replacement / retirement / improvement
- Schedule: abrupt
- Changed-mass target: **two flags plus one new shortcut**
- Invariant: target output remains fixed; command route/cost may change
- Stale failure:
  - old recipe may fail;
  - repaired conservative recipe may remain correct but suboptimal
- Evidence:
  - D1 = 50% changelog
  - D2 = 0%
  - D3 = 50% help text + probes
- Required update: repair tool model and recipes; promote only after output/cost validation
- Validation: IAR, output correctness, RR/CRR, probes, BE
- Readouts: IAR, RR, CRR, `d*`, BE

BFCL's `GorillaFileSystem` already provides command-like operations (`find`, `cp`, `mv`, `cat`, `wc`, etc.). However, native BFCL exposes them as structured Python-style functions rather than a true command grammar. To make C3.1 genuinely about **CLI flag semantics**, create a thin BFCL-compatible CLI adapter over `GorillaFileSystem`.

---

## 1. Select the BFCL source tasks

Use file-system tasks such as:

```text
multi_turn_base_19
```

which asks the agent to:

1. find `test_document.txt`;
2. copy it to an archives folder and rename it;
3. read the renamed file.

Also use tasks such as:

```text
multi_turn_base_25
```

which exercise:

```text
find
cp
sort
wc
```

These are useful because the final filesystem/output semantics can be kept fixed while CLI syntax and route cost drift.

Create:

```bash
git switch -c drift-c3-1
mkdir -p drift_benchmark/C3.1/cli_semantics
```

---

## 2. Keep `GorillaFileSystem` as the execution backend

Do not reimplement filesystem semantics.

Reuse:

```text
bfcl_eval/eval_checker/multi_turn_eval/func_source_code/gorilla_file_system.py
```

as the underlying stateful filesystem.

The new CLI layer should translate a command string into calls on this existing backend.

This preserves BFCL state transitions and evaluation behavior.

---

## 3. Add a new BFCL backend class: `GorillaCLI`

Create:

```text
bfcl_eval/eval_checker/multi_turn_eval/func_source_code/gorilla_cli.py
```

Expose one primary function:

```python
run(command: str) -> dict
```

Internally:

1. tokenize the command string;
2. parse it according to the active CLI regime;
3. translate it into one or more `GorillaFileSystem` operations;
4. execute them;
5. return stdout/error/exit-code metadata.

Suggested response:

```json
{
  "stdout": "...",
  "stderr": "",
  "exit_code": 0,
  "operations": 1
}
```

Do not expose the full private grammar in the standard function schema.

---

## 4. Add a tool document for the adapter

Create:

```text
bfcl_eval/data/multi_turn_func_doc/gorilla_cli.json
```

Minimal schema:

```json
{
  "name": "run",
  "description": "Execute one command under the current private filesystem CLI grammar.",
  "parameters": {
    "type": "dict",
    "properties": {
      "command": {
        "type": "string",
        "description": "A command string using the current CLI version."
      }
    },
    "required": ["command"]
  }
}
```

Also expose a metered help/probe action, either as:

```text
help(topic)
```

or through:

```text
run("help find")
```

The exact current flags must not be leaked in the normal tool schema.

---

## 5. Register `GorillaCLI` in BFCL

Extend:

```text
bfcl_eval/constants/executable_backend_config.py
```

with a mapping for:

```text
GorillaCLI
```

to:

```text
gorilla_cli.json
gorilla_cli.py
```

Keep the change isolated and backwards compatible.

---

## 6. Define the `r-` command grammar

Create:

```text
grammar_r_minus.json
```

Use a small grammar sufficient for the selected tasks.

Example:

```text
find --path <directory> --name <name>
cat <file>
cp <source> <destination>
mv <source> <destination>
wc -l <file>
sort <file>
```

For `multi_turn_base_19`, the baseline route may be:

```text
find --path . --name test_document.txt
cp test_document.txt archives
cd archives
mv test_document.txt final_document.txt
cat final_document.txt
```

You may instead keep BFCL's current-directory restrictions and model `cd` explicitly.

Define operation cost as the number of underlying filesystem operations, not merely the number of high-level tool calls.

---

## 7. Define the exact C3.1 physical drift

The roster calls for:

```text
two flags plus one new shortcut
```

Use:

### Flag remap

```text
find --path <dir>
```

becomes:

```text
find --root <dir>
```

Old `--path` is invalid in `r+`.

### Flag deprecation

```text
wc -l <file>
```

is retired.

The new valid form is:

```text
wc --lines <file>
```

`-l` must return a deprecation/invalid-option error in `r+`.

### Shortcut addition

Add a new command:

```text
copy-as <source> --to <directory> --name <new_name>
```

This performs the same semantic operation as:

```text
cp + cd/mv
```

but at lower cost.

Example:

```text
copy-as test_document.txt --to archives --name final_document.txt
```

The final filesystem state is identical to the old multi-command route.

Create:

```text
drift_spec.json
```

with:

```json
{
  "operator": ["flag_remap", "deprecate", "shortcut_add"],
  "schedule": "abrupt",
  "changes": [
    {
      "command": "find",
      "old_flag": "--path",
      "new_flag": "--root",
      "type": "remap"
    },
    {
      "command": "wc",
      "old_flag": "-l",
      "new_flag": "--lines",
      "type": "deprecate"
    },
    {
      "command": "copy-as",
      "type": "shortcut_add",
      "replaces_route": ["cp", "mv"]
    }
  ]
}
```

---

## 8. Implement the `r-` parser

Create:

```text
gorilla_cli_r_minus.py
```

Supported examples:

```text
find --path . --name test_document.txt
wc -l summary.txt
cp source destination
mv source destination
cat file
sort file
```

Translate into existing BFCL filesystem methods.

Example:

```text
find --path . --name x
```

maps to:

```python
filesystem.find(path=".", name="x")
```

---

## 9. Implement the `r+` parser

Create:

```text
gorilla_cli_r_plus.py
```

Rules:

```text
--path    → error
--root    → valid

-l        → deprecated/error
--lines   → valid

copy-as   → valid and lower-cost
```

Implement `copy-as` by executing the underlying `cp` and `mv`/rename logic internally, but charge it as a deliberately cheaper route according to the benchmark cost model.

Keep the final filesystem state exactly equivalent.

---

## 10. Define the cost model explicitly

Because C3.1 includes an improvement operator, the cost model must distinguish:

```text
old correct route
new shortcut route
```

Recommended cost:

```text
each underlying primitive filesystem operation = 1
help query = 1
invalid probe = 1
copy-as shortcut = 1
```

Thus:

```text
r- copy + rename:
cp + mv = 2

r+ copy-as:
copy-as = 1
```

Record both:

```text
semantic correctness
execution cost
```

Never score only the number of LLM tool-call messages if the wrapper expands one command internally.

---

## 11. Hide current grammar from ordinary task prompts

Convert BFCL source prompts to semantic instructions.

For example:

```text
"Find test_document.txt, copy it into archives as final_document.txt,
then show me its contents."
```

Do not write:

```text
--path
--root
-l
--lines
copy-as
```

in the user request.

The persistent skill stores the CLI grammar.

---

## 12. Create stale and oracle CLI skills

Create:

```text
skill_r_minus.md
skill_r_plus_oracle.md
```

`skill_r_minus.md`:

```markdown
# Gorilla CLI Skill — r-

## Find
`find --path <directory> --name <name>`

## Count lines
`wc -l <file>`

## Copy and rename
1. `cp <source> <destination>`
2. rename/move the copied file with `mv`
```

`skill_r_plus_oracle.md`:

```markdown
# Gorilla CLI Skill — r+

## Find
`find --root <directory> --name <name>`

## Count lines
`wc --lines <file>`

## Copy and rename
Preferred:
`copy-as <source> --to <directory> --name <new_name>`

Fallback:
use compatible primitive commands only if the shortcut is unavailable.
```

Do not give the oracle skill to the learner.

---

## 13. Build C3.1 D1 changelog evidence

D1 carries 50% of identification weight.

Create:

```text
evidence/d1_changelog.md
```

The changelog should **localize the affected commands** but should not provide all exact replacement semantics.

Example:

```markdown
# Gorilla CLI v2 Migration Notice

The following command interfaces changed:

- The directory-selection option for `find` has been renamed.
- The short line-count option for `wc` has been retired.
- A new optimized copy-and-rename operation is available.

Use current command help to determine the exact replacement syntax and
validate behavior before updating persistent command recipes.
```

This identifies:

```text
find
wc
copy/rename workflow
```

but does not tell the learner:

```text
--root
--lines
copy-as syntax
```

That leaves meaningful D3 obligations.

---

## 14. Build C3.1 D3 help and probe evidence

D3 carries the remaining 50%.

Implement metered help:

```text
help find
help wc
help copy-as
```

Example response:

```text
find:
  --root <directory>
  --name <name>
```

Example:

```text
wc:
  --lines <file>
```

Example:

```text
copy-as:
  copy-as <source> --to <directory> --name <new_name>
  estimated primitive cost: 1
```

Also allow execution probes.

Probe stale syntax:

```text
find --path . --name test_document.txt
```

returns:

```text
error: unknown option --path
```

Probe:

```text
wc -l summary.txt
```

returns:

```text
error: option -l is deprecated
```

The learner should validate the new syntax before storing it.

---

## 15. Define evidence obligations

Create:

```text
evidence_allocation.json
```

Example obligations:

```text
O1: identify that find syntax changed
O2: identify exact replacement flag --root
O3: identify that wc -l is retired
O4: identify exact replacement --lines
O5: identify existence of copy/rename shortcut
O6: identify exact copy-as syntax
O7: determine shortcut cost advantage
```

Assign:

```text
D1:
O1, O3, O5

D3:
O2, O4, O6, O7
```

This gives a natural 50/50 split.

---

## 16. Build BFCL sibling tasks

Create:

```text
siblings.json
```

Use modified siblings based on BFCL file-system tasks.

### Affected example — flag remap

Semantic request:

```text
Find config.py anywhere under the current project.
```

Requires current `find` grammar.

### Affected example — deprecation

Semantic request:

```text
Count the lines in summary.txt.
```

Requires current `wc` grammar.

### Affected example — shortcut

Semantic request:

```text
Copy test_document.txt to archives and rename the copy
final_document.txt.
```

Both old and new routes can be semantically correct, but the new route is cheaper.

### Unchanged-retention

Examples using:

```text
cat
sort
grep
```

whose grammar is unchanged.

### Boundary/open-set

Unsupported option or unsupported command request.

The learner should use help/abstain rather than invent syntax.

### Matched null

Change filenames/content while keeping grammar and route unchanged.

---

## 17. Compile paired gold behavior

Create:

```text
possible_answer_r_minus.json
possible_answer_r_plus.json
```

For output-correctness tasks, semantic gold remains fixed.

Example:

```text
same final file:
archives/final_document.txt

same content:
original test_document content
```

But command gold changes.

`r-`:

```text
find --path ...
cp ...
mv ...
cat ...
```

`r+` oracle:

```text
find --root ...
copy-as ...
cat ...
```

For line counts:

`r-`:

```text
wc -l summary.txt
```

`r+`:

```text
wc --lines summary.txt
```

---

## 18. Define the intended skill update

Expected patch:

```diff
- find --path <dir> --name <name>
+ find --root <dir> --name <name>

- wc -l <file>
+ wc --lines <file>
+ tombstone: wc -l

+ preferred copy+rename:
+ copy-as <source> --to <dir> --name <new_name>
```

The updater should not modify unchanged commands.

---

## 19. Validate output before promoting a new recipe

For each candidate new recipe:

1. execute it in `r+`;
2. verify the final filesystem/output;
3. measure cost;
4. only then write it into persistent skill.

This directly implements the roster requirement:

```text
repair tool model and recipes,
then promote only after output and cost validation
```

---

## 20. Run the required controls

### A. No skill

Must rediscover grammar.

### B. Oracle `S+`

Current syntax and optimal routes.

### C. Stale `S-`

Expected:
- invalid `--path`;
- invalid/deprecated `-l`;
- correct but slower copy+rename primitive route.

### D. Stale `S-` + D1 + D3 + updater

Target condition.

### Ablations

Also run:

```text
D1 only
D3 only
D1 + D3
```

because the setting is a mixed evidence construction.

---

## 21. Metrics

### IAR

Invalid command/flag actions divided by executable attempts.

### RR

Correctness recovery for syntax changes that otherwise cause failure.

### CRR

Cost recovery for the new shortcut at matched final output.

### `d*`

Minimum help/probe budget needed to identify current grammar sufficiently.

### BE

Break-even horizon:

```text
future saved command cost
>
one-time discovery/update cost
```

Do not claim BE until repeated post-drift siblings exist.

---

## 22. Scale toward roster task counts

Start with approximately:

```text
18–24 tasks
```

then scale toward:

```text
60 total
24 affected
18 unchanged-retention
12 boundary/open-set
6 matched-null
```

Ensure at least two decisive siblings per changed obligation before scaling further.

---

## 23. Acceptance checklist

- [ ] underlying filesystem semantics are still BFCL `GorillaFileSystem`;
- [ ] CLI adapter is executable and stateful;
- [ ] user prompts state semantic goals, not current flags;
- [ ] normal function docs do not leak the private grammar;
- [ ] exactly two flag changes are implemented;
- [ ] one old flag is explicitly deprecated;
- [ ] one semantically equivalent faster command is added;
- [ ] final output/filesystem state remains invariant;
- [ ] D1 localizes changed commands but leaves exact syntax unresolved;
- [ ] D3 help/probes reveal exact syntax and cost;
- [ ] D2 is withheld;
- [ ] stale skill exhibits both failure and suboptimal-but-correct cases;
- [ ] candidate updates are execution-validated before storage;
- [ ] unchanged commands show negligible collateral edits.

---

## 24. Expected output package

```text
drift_benchmark/C3.1/cli_semantics/
├── README.md
├── drift_spec.json
├── grammar_r_minus.json
├── grammar_r_plus.json
├── gorilla_cli_r_minus.py
├── gorilla_cli_r_plus.py
├── gorilla_cli.json
├── skill_r_minus.md
├── skill_r_plus_oracle.md
├── siblings.json
├── possible_answer_r_minus.json
├── possible_answer_r_plus.json
├── siblings.json
├── evidence/
│   ├── d1_changelog.md
│   ├── d3_help.jsonl
│   ├── d3_probe_log.jsonl
│   └── evidence_allocation.json
└── validation/
    ├── cli_replay.py
    ├── output_checks.py
    └── construction_report.json
```

## Minimal conceptual summary

```text
r- CLI grammar

find --path ...
wc -l ...
cp ... + mv ...


             abrupt drift


r+ CLI grammar

find --root ...        # remapped flag
wc --lines ...         # -l retired
copy-as ...            # new fast path


Evidence

D1 50%:
"find changed"
"wc short flag retired"
"new copy/rename command exists"

D3 50%:
help/probes reveal --root, --lines,
copy-as syntax and cost

             |
             v
      validated skill update
```
